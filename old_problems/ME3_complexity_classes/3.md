1. (25 pts) Suppose we are performing the sat reduction for a problem. $S_{t, i, a}$ is true and $S_{t + 1, i, a}$ is true. Is is possible for both variables to be true (10 pts)? Explain your answer (15 pts)?

2. (25 pts) Think back to project 5 where we computed the shortest path between two points in a tile-based graph. Suppose we formulate the problem as "is this path the shortest path?" Of the following classes: P, NP-Inter, NP-complete, NP, EXPTIME: Which is the most specific complexity class we can put this problem in if we assume P != NP (10 pts), and why (15 pts)? By specific, I mean choose the first class in that list which could contain the problem.

3. (25 pts) Suppose that $A \le TSP$ and the best known reduction is in exponential time. From this, can we conclude that $A \in NP$ (10 pts)? Why or why not (15 pts)?

4. (25 pts) Suppose we have a 100-node graph encoded as an adjacency matrix. Every cell of the matrix is filled in with positive weights. What can we say about the size of the largest clique of this graph (10 pts) and why (15 pts)?

